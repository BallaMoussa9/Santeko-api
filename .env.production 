# =========================================================
# 🌍 Application
# =========================================================
APP_NAME="SanTeko"
APP_ENV=production
APP_KEY=base64:s5zVP7kOZfWYhBxElnaTSotsgAajo+06AgLmAOHu/Ds=
APP_DEBUG=false
APP_URL=${RENDER_EXTERNAL_URL}

APP_LOCALE=fr
APP_FALLBACK_LOCALE=fr
APP_FAKER_LOCALE=fr_FR

# =========================================================
# 🪵 Logs
# =========================================================
LOG_CHANNEL=stack
LOG_LEVEL=info

# =========================================================
# 🗄️ Base de Données (Render PostgreSQL)
# =========================================================
DB_CONNECTION=pgsql
DB_HOST=${DB_HOST}           # fourni par Render automatiquement
DB_PORT=5432                 # port PostgreSQL
DB_DATABASE=${DB_NAME}       # fourni par Render
DB_USERNAME=${DB_USER}       # fourni par Render
DB_PASSWORD=${DB_PASS}       # fourni par Render

# =========================================================
# 🍪 Sessions & Cache
# =========================================================
SESSION_DRIVER=cookie
SESSION_LIFETIME=120
CACHE_DRIVER=file
QUEUE_CONNECTION=sync

# =========================================================
# ✉️ Email (Gmail SMTP)
# =========================================================
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=ballamoussasinayoko@gmail.com
MAIL_PASSWORD=${MAIL_PASSWORD}   # tu la définiras manuellement dans Render
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=ballamoussasinayoko@gmail.com
MAIL_FROM_NAME="SanTeko"

# =========================================================
# ⚡️ Frontend (optionnel)
# =========================================================
VITE_APP_NAME="${APP_NAME}"
